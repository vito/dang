# local variable rename
let renamable = 42

renamable # test: ^grn<C-u>world<CR> => ┃world

# function parameter rename
add(x: Int!, y: Int!): Int! { x + y } # test: ^fxgrn<C-u>a<CR> => add(┃a: Int!, y: Int!): Int! { a + y }

# function rename
sub(x: Int!, y: Int!): Int! { x - y } # test: ^wgrn<C-u>minus<CR> => ┃minus(a: Int!, y: Int!): Int! { a - y }

add

# multiple occurrences
let occurrences = 10

occurrences + occurrences + occurrences # test: ^grn<C-u>bar<CR> => ┃bar + bar + bar
