# imported binding
use ./defs.dang as defs

defs.adele ; test: wgd => pub ┃adele = "hello from the other side"

# local binding
hello = 42

hello ; test: gd => ┃hello = 42

# function definition
add_nums = fn(x, y) {
  x + y ; test: wgd => add_nums = fn(┃x, y) {
}

add_nums ; test: gd => ┃add_nums = fn(x, y) {

# nested scope
outer = 1
inner_fn = fn() {
  inner = 2
  outer + inner ; test: wwgd => ┃outer = 1
}
inner_fn ; test: gd => ┃inner_fn = fn() {
