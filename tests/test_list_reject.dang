# Test list.reject() method

# Basic functionality - reject even numbers
numbers: [Int!]! = [1, 2, 3, 4, 5]
odds: [Int!]! = numbers.reject { x => x % 2 == 0 }
assert { odds[0] == 1 }
assert { odds[1] == 3 }
assert { odds[2] == 5 }

# Reject strings that start with 'h'
words: [String!]! = ["hello", "world", "hi", "there"]
filtered: [String!]! = words.reject { w => w.hasPrefix("h") }
assert { filtered[0] == "world" }
assert { filtered[1] == "there" }

# Reject all elements
all_rejected: [Int!]! = [1, 2, 3].reject { x => true }
assert { all_rejected == [] }

# Reject no elements
none_rejected: [Int!]! = [1, 2, 3].reject { x => false }
assert { none_rejected == [1, 2, 3] }

print("List reject tests passed!")
