# Test simple 2-level deep assignment

cls Inner {
  pub value = "original"
}

cls Outer {
  pub inner = Inner { }
  
  pub update: Outer! {
    self.inner.value = "updated"
    self
  }
}

pub result = Outer { }.update
assert { result.inner.value == "updated" }

print("Simple deep assignment test passed!")