type Calculator {
  pub x: Int!

  """
  Start a coding agent with the provided module.
  """
  pub addY(y: Int!): Calculator! {
    self.x += y
    self
  }
}

assert { Calculator(1).addY(4).x == 5 }
assert { toJSON(Calculator(1).addY(4)) == "{\"x\":5}" }
