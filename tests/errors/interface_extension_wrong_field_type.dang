# A child interface's field type must be compatible with the parent's.

interface Named {
  name: String!
}

# Wrong: parent requires String!, child weakens to nullable String
interface Person implements Named {
  name: String
}
