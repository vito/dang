# Local fields on a type that shadows an imported GraphQL type must not be
# accepted inside inline fragments for the imported union member.

import Test

type User {
  name: String!
  origin: String! = "local"
}

let bad = search(query: "John").{{
  ... on User { origin }
}}
