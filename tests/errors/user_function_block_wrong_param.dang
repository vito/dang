# Test: block parameter type incompatible with usage

processor(&block(x: String!): Int!): Int! {
  block("hello")
}

# ERROR: cannot use String! in numeric operation
result: Int! = processor { x => x * 2 }
