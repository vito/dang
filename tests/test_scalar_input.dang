import Test

# String literals auto-coerce into custom GraphQL scalars at call boundaries.

let result = fetchURL(url: "https://example.com")
assert { result == "Fetched: https://example.com" }

print("Scalar input tests passed!")
