import Test

# Test GraphQL scalar type as function input. Custom scalars require explicit
# casts from String values.

pub result = fetchURL(url: "https://example.com" :: URL!)
assert { result == "Fetched: https://example.com" }

print("Scalar input tests passed!")
