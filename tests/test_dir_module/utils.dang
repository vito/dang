# Utility functions using types from types.dang
createPerson(name: String!, age: Int!): Person! {
  Person(name, age)
}

getMagicNumber: Int! {
  MAGIC_NUMBER
}

formatVersion: String! {
  "Version: " + VERSION
}
