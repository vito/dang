import Test

assert("auto-calling symbols") {
  Test.serverInfo().platform() == Test.serverInfo.platform()
}

assert("auto-calling selections") {
  Test.serverInfo.platform == Test.serverInfo.platform()
}
