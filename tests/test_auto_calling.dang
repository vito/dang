import Test

assert("auto-calling symbols") {
  serverInfo().platform() == serverInfo.platform() #nofmt
}

assert("auto-calling selections") {
  serverInfo.platform == serverInfo.platform() #nofmt
}

assert("auto-calling selections (qualified)") {
  Test.serverInfo().platform() == Test.serverInfo.platform() #nofmt
}
