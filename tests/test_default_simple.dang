# Test default operator basic cases

# Simple null coalescing in expression context
assert { (null ?? "default") == "default" }
assert { ("value" ?? "default") == "value" }
assert { (null ?? 42) == 42 }

print("Default operator simple tests passed!")