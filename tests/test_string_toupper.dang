# Test string toUpper method

let upper = "hello world".toUpper
assert { upper == "HELLO WORLD" }

let mixed = "HeLLo".toUpper
assert { mixed == "HELLO" }

let already = "ALREADY".toUpper
assert { already == "ALREADY" }

print("String.toUpper() tests passed!")
