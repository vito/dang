cls Dash {
  pub source: Directory!

  pub new(
    source: Directory!
  ): Dash! {
    self.source = source
    self
  }

  pub build(): File! {
    go(source).binary("./cmd/dash")
  }

  pub test(): Container! {
    go(source).base.
      withDirectory("/src", source).
      withWorkdir("/src").
      withExec(["go", "test", "-v", "./..."], experimentalPrivilegedNesting: true)
  }
}
