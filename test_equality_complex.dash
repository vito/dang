# Test complex equality scenarios

# Test lists
pub list1 = [1, 2, 3]
pub list2 = [1, 2, 3]  
pub list3 = [1, 2, 4]
assert("Identical lists should be equal") { list1 == list2 }
assert("Different lists should not be equal") { list1 == list3 }

print("Complex equality tests completed")