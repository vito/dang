# Test assertions with more detailed output

# Test with simple passing assertion
pub x = 42
assert { x }

# Test with symbol that should show its value
pub message = "hello"
assert("Message exists") { message }

# Test conditional that will fail to show child values
pub isActive = false
assert("Should be active") { 
  if isActive { "yes" } else { null }
}